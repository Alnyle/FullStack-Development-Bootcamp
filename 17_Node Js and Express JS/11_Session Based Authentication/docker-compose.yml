services:
  db:
    image: postgres:18.2
    ports:
      - "5435:5432"
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: mypassword
      POSTGRES_DB: postgres
    volumes:
      - db_data:/var/lib/postgresql

volumes:
  db_data: