import os from 'os';

// userInfo()
console.log(os.userInfo());

// totalmem : total memory space
console.log(os.totalmem());

// freemem: free memory
console.log(os.freemem());


// cpus()
console.log(os.cpus());